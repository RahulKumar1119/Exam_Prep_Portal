/** @type {import('tailwindcss').Config} */
module.exports = {
  // Tailwind v4 uses CSS-first config via @theme in src/index.css
  // This file is kept for compatibility; theme is now defined in CSS.
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
};
